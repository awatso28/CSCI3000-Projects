#include <iostream>
using namespace std;
int main()
{
    /*
    //Input and Output of Strings Example
    char user_name[50];
    cout << "Enter your full name: ";
    cin.getline(user_name, 50);
    cout << "Hello, " << user_name << "!" << endl;
    //String Length Example
    char name[50];
    cout << "Enter your name: ";
    cin.getline(name, 50);
    cout << "Length of the name: " << strlen(name) << endl;
    //String Length 2nd Example
    char username[50];
    int length = 0;
    cout << "Enter your name: ";
    cin.getline(username, 50);
    while (username[length] != '\0')
    {
        length++;
    }
    cout << "Length of the name: " << length << endl;
    //Copy a String Example
    char source[50];
    char destination[50];
    int index = 0;
    cout << "Enter a name: ";
    cin.getline(source, 50);
    while (source[index] != '\0')
    {
        destination[index] = source[index];
        index++;
    }
    destination[index] = '\0';
    cout << "Copies string: " << destination << endl;
    //Computers Understand Letters Example
    char character_value;
    character_value = 'A';
    int code;
    code = character_value;
    cout << "Character: " << character_value << endl;
    cout << "ASCII Code: " << code << endl;
    //Manual String Comparison Logic Example
    char first_word[20];
    char second_word[20];
    cout << "Enter first word: ";
    cin >> first_word;
    cout << "Enter second word: ";
    cin >> second_word;
    int index = 0;
    int result = 0;
    while (first_word[index] != '\0' && second_word[index] != '\0')
    {
        if (first_word[index] != second_word[index])
        {
            result = first_word[index] - second_word[index];
            break;
        }
        index++;
    }
    if (result == 0)
    {
        result = first_word[index] - second_word[index];
    }
    if (result == 0)
    {
        cout << "Both strings are equal\n";
    }
    else if (result < 0)
    {
        cout << "First string is smaller\n";
    }
    else
        cout << "First string is greater\n";
    //String Compare Function Example
    char first_word[20];
    char second_word[20];
    cout << "Enter first word: ";
    cin >> first_word;
    cout << "Enter second word: ";
    cin >> second_word;
    int result = strcmp(first_word, second_word);
    if (result == 0)
    {
        cout << "Both strings are equal\n";
    }
    else if (result < 0)
    {
        cout << "First string is smaller\n";
    }
    else
    {
        cout << "First string is greater\n";
    }
    //Manual String Concatenation Example
    char first_string[50];
    char second_string[50];
    int first_index = 0;
    int second_index = 0;
    cout << "Enter first string: ";
    cin.getline(first_string, 50);
    cout << "Enter second string: ";
    cin.getline(second_string, 50);
    while (first_string[first_index] != '\0')
    {
        first_index++;
    }
    while (second_string[second_index] != '\0')
    {
        first_string[first_index] = second_string[second_index];
        first_index++;
        second_index++;
    }
    first_string[first_index] = '\0';
    cout << "Concatenated string: " << first_string << endl;
    //Using strcat() Example
    char first_string[30] = "Hello";
    char second_string[] = "World";
    strcat_s(first_string, second_string);
    cout << "After strcat: " << first_string << endl;
    //Practice 32: Reverse a String
    char text[200];
    cout << "Enter a word: ";
    cin.getline(text, 200);
    int length = 0;
    while (text[length] != '\0')
    {
        length++;
    }
    int left_index = 0;
    int right_index = length - 1;
    while (left_index < right_index)
    {
        char temp = text[left_index];
        text[left_index] = text[right_index];
        text[right_index] = temp;
        left_index++;
        right_index++;
    }
    cout << text << endl;
    return 0;
}

