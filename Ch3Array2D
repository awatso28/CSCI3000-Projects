#include <iostream>
using namespace std;

//Ex2 constants for array size
//const int max_row = 3;
//const int max_col = 3;
//Ex2 function declaratin
//void read_array(int marks[][max_col], int max_row);
//P1 constants for array size
const int max_row = 2;
const int max_col = 3;
//P1 Function declarations
void read_array(int marks[][max_col], int max_row);
void print_array(int marks[][max_col], int max_row);
int main()
{
    /*
    //First Example
    int marks[3][3];
    for (int row = 0; row < 3; row++)
    {
        for (int col = 0; col < 3; col++)
        {
            cin >> marks[row][col];
        }
    }
    for (int row = 0; row < 3; row++)
    {
        for (int col = 0; col < 3; col++)
        {
            cin >> marks[row][col];
        }
        cout << endl;
    }
    
    //Second Example
    int marks[max_row][max_col];
    read_array(marks, max_row);
    */
    //Practice 1
    int marks[max_row][max_col];
    read_array(marks, max_row);
    print_array(marks, max_row);
    return 0;
}
//Ex2 function definiton
/*
void read_array(int marks[][max_col], int max_row)
{
    for (int row = 0; row < max_row; row++)
    {
        for (int col = 0; col < max_col; col++)
        {
            cout << "Enter value for marks[" << row << "][" << col << "]: ";
            cin >> marks[row][col];
        }
    }
}
*/

void read_array(int marks[][max_col], int max_row)
{
    for (int row = 0; row < max_row; row++)
    {
        for (int col = 0; col < max_col; col++)
        {
            cout << "Enter value for marks[" << row << "][" << col << "]: ";
            cin >> marks[row][col];
        }
    }
}

void print_array(int marks[][max_col], int max_row)
{
    cout << "\nMarks array:\n";
    for (int row = 0; row < max_row; row++)
    {
        for (int col = 0; col < max_col; col++)
        {
            cout << marks[row][col] << " ";
        }
        cout << endl;
    }
}
